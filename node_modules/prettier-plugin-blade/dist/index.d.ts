import { Plugin, SupportOption } from 'prettier';

declare const languages: Plugin["languages"];
declare const parsers: Plugin["parsers"];
declare const printers: Plugin["printers"];
/**
 * Declare HTML-specific options so Prettier passes them through.
 */
declare const options: Record<string, SupportOption>;
declare const plugin: Plugin;

export { plugin as default, languages, options, parsers, printers };
